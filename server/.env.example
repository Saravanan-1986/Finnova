PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/finnova
JWT_SECRET=change_this_to_a_long_random_string
CLIENT_URL=http://localhost:5173

# AI Assistant — add at least one provider key, or the assistant falls back
# to a built-in local rule-based responder that still cites real user data.
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional: messages per user per hour (default 20)
ASSISTANT_RATE_LIMIT=20